{
  "name": "calcettinho",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "npm install && cd frontend && npm install && npm run build",
    "build:netlify": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start",
    "dev": "cd frontend && npm run dev",
    "migrate:airtable-to-supabase": "tsx scripts/migrate-airtable-to-supabase.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "airtable": "^0.12.2",
    "dotenv": "^16.5.0",
    "googleapis": "^149.0.0"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "tsx": "^4.19.2"
  }
} 